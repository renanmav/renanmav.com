import { Inter, Fira_Code } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const fira = Fira_Code({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-fira",
});
